<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="
		http://maven.apache.org/POM/4.0.0
		http://maven.apache.org/maven-v4_0_0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <groupId>be.fluid-it.tools.dropwizard.it</groupId>
    <artifactId>wizard-in-a-box-integration-tests</artifactId>
    <version>testing</version>
    <packaging>pom</packaging>

    <modules>
        <module>test-support</module>
        <module>sample-servlets</module>
        <module>sample-assets</module>
        <module>sample-web-resources</module>
        <module>sample-wiz-app</module>
        <module>sample-wiz-swagger-app</module>
        <module>sanity-checks-parent</module>
        <module>sanity-check-sample-jee-war</module>
        <module>sanity-check-sample-wiz-app-war</module>
        <module>sanity-check-sample-wiz-swagger-app-war</module>
    </modules>

    <properties>
      <!-- Only needed to enable running from the IDE -->
      <!-- When building wizard-in-a-box the mvn-invoker-plugin will propagate the versions from the wizard-in-a-box pom -->
      <dropwizard.version>1.1.0</dropwizard.version>
      <dropwizard.slf4j.version>1.7.12</dropwizard.slf4j.version>
      <dropwizard.logback.version>1.2.3</dropwizard.logback.version>
      <wizard.in.a.box.version>1.1-1-SNAPSHOT</wizard.in.a.box.version>
    </properties>

    <dependencyManagement>
        <dependencies>
            <dependency>
                <groupId>be.fluid-it.tools.dropwizard</groupId>
                <artifactId>wiz-to-war</artifactId>
                <version>testing</version>
            </dependency>
            <dependency>
                <groupId>io.dropwizard</groupId>
                <artifactId>dropwizard-core</artifactId>
                <version>${dropwizard.version}</version>
            </dependency>
            <dependency>
                <groupId>javax.servlet</groupId>
                <artifactId>javax.servlet-api</artifactId>
                <version>3.1.0</version>
            </dependency>
            <dependency>
                <groupId>org.slf4j</groupId>
                <artifactId>slf4j-api</artifactId>
                <version>${dropwizard.slf4j.version}</version>
            </dependency>
            <dependency>
                <groupId>ch.qos.logback</groupId>
                <artifactId>logback-classic</artifactId>
                <version>${dropwizard.logback.version}</version>
            </dependency>
        </dependencies>
    </dependencyManagement>
    <dependencies>
        <dependency>
            <groupId>junit</groupId>
            <artifactId>junit</artifactId>
            <version>4.12</version>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>be.fluid-it.tools.dropwizard.it</groupId>
            <artifactId>test-support</artifactId>
            <version>testing</version>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>commons-io</groupId>
            <artifactId>commons-io</artifactId>
            <version>1.3.2</version>
            <scope>test</scope>
        </dependency>
    </dependencies>
    <build>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-jar-plugin</artifactId>
                <version>2.5</version>
                <executions>
                    <execution>
                        <goals>
                            <goal>test-jar</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>
        </plugins>
        <pluginManagement>
            <plugins>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-war-plugin</artifactId>
                    <version>2.3</version>
                    <configuration>
                        <failOnMissingWebXml>false</failOnMissingWebXml>
                    </configuration>
                </plugin>
            </plugins>
        </pluginManagement>
    </build>
</project>